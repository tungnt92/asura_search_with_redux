// import { fromJS } from 'immutable';
// import { makeSelectPersonalFormDomain } from '../selectors';

// const selector = makeSelectPersonalFormDomain();

describe('makeSelectPersonalFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
